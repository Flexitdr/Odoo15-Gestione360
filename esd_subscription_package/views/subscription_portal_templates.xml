<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <template
        id="portal_layout"
        name="Portal layout: subscription menu entry"
        inherit_id="portal.portal_breadcrumbs"
        priority="50"
    >
        <xpath expr="//ol[hasclass('o_portal_submenu')]" position="inside">
            <li
                t-if="page_name == 'subscription' or subscription"
                t-attf-class="breadcrumb-item #{'active ' if not subscription else ''}"
            >
                <a
                    t-if="subscription"
                    t-attf-href="/my/subscriptions?{{ keep_query() }}"
                >Subscriptions</a>
                <t t-else="">Subscriptions</t>
            </li>
            <li t-if="subscription" class="breadcrumb-item active">
                <t t-esc="subscription.name" />
            </li>
        </xpath>
    </template>
    <template
        id="portal_my_home"
        name="Portal My Home : subscription "
        inherit_id="portal.portal_my_home"
        priority="40"
    >
        <xpath expr="//div[hasclass('o_portal_docs')]" position="inside">
            <t t-call="portal.portal_docs_entry">
                <t t-set="title">Subscriptions</t>
                <t t-set="url" t-value="'/my/subscriptions'" />
                <t t-set="placeholder_count" t-value="'subscription_count'" />
            </t>
        </xpath>
    </template>
    <template id="portal_my_subscriptions" name="My tickets">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True" />

            <t t-if="not subscriptions">
                <p>There are no subs in your account.</p>
            </t>
            <div t-if="subscription" class="panel panel-default">
                <div class="table-responsive">
                    <table class="table table-hover o_portal_my_doc_table">
                        <thead>
                            <tr class="active">
                                <th>By</th>
                                <th>Number</th>
                                <th>Name</th>
                                <th>Category</th>
                                <th>Stage</th>
                                <th>Create Date</th>
                                <th>Last Stage Update</th>
                                <th>Close Date</th>
                            </tr>
                        </thead>
                        <t t-foreach="tickets" t-as="ticket">
                            <tr>
                                <td>
                                    <t t-esc="ticket.partner_id.name" />
                                </td>
                                <td>
                                    <t t-esc="ticket.number" />
                                </td>
                                <td>
                                    <a t-attf-href="/my/ticket/#{ticket.id}">
                                        <t t-esc="ticket.name" />
                                    </a>
                                </td>
                                <td>
                                    <t t-esc="ticket.category_id.name" />
                                </td>
                                <td>
                                    <t t-esc="ticket.stage_id.name" />
                                </td>
                                <td>
                                    <span t-field="ticket.create_date" />
                                </td>
                                <td>
                                    <span t-field="ticket.last_stage_update" />
                                </td>
                                <td>
                                    <span t-field="ticket.closed_date" />
                                </td>
                            </tr>
                        </t>
                    </table>
                </div>
                <div t-if="pager" class="o_portal_pager text-center">
                    <t t-call="portal.pager" />
                </div>
            </div>
        </t>
    </template>

    <template id="portal_create_ticket" name="Create Ticket">
        <t t-call="portal.portal_layout">
            <div class="container">
                <div class="row">
                    <div class="col-md-12">
                        <h1 class="text-center">Send a new ticket</h1>
                    </div>
                </div>
            </div>
            <form
                action="/submitted/ticket"
                method="POST"
                class="form-horizontal mt32"
                enctype="multipart/form-data"
            >
                <input
                    type="hidden"
                    name="csrf_token"
                    t-att-value="request.csrf_token()"
                />
                <div class="form-group">
                    <label
                        class="col-md-3 col-sm-4 control-label"
                        for="category"
                    >Category</label>
                    <div class="col-md-7 col-sm-8">
                        <select
                            class="form-control"
                            id="category"
                            name="category"
                            required="True"
                        >
                            <t t-foreach="categories" t-as="cat">
                                <option t-attf-value="#{cat.id}">
                                    <t t-esc="cat.name" />
                                </option>
                            </t>
                        </select>
                    </div>
                </div>
                <div class="form-group">
                    <label
                        class="col-md-3 col-sm-4 control-label"
                        for="subject"
                    >Subject</label>
                    <div class="col-md-7 col-sm-8">
                        <input
                            type="text"
                            class="form-control"
                            name="subject"
                            required="True"
                        />
                    </div>
                </div>
                <div class="form-group">
                    <label
                        class="col-md-3 col-sm-4 control-label"
                        for="attachment"
                    >Add Attachments</label>
                    <div class="col-md-7 col-sm-8">
                        <div class="btn btn-default btn-file col-md-12">
                            <input
                                class="form-control o_website_form_input"
                                name="attachment"
                                id="attachment"
                                type="file"
                                multiple="multiple"
                            />
                        </div>
                    </div>
                </div>
                <div class="form-group">
                    <label
                        class="col-md-3 col-dm-4 control-label"
                        for="description"
                    >Description</label>
                    <div class="col-md-7 col-sm-8">
                        <textarea
                            class="form-control"
                            name="description"
                            style="min-height: 120px"
                            required="True"
                        />
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-md-offset-3 col-sm-offset-4 col-sm-8 col-md-7">
                        <button class="btn btn-primary btn-lg">Submit Ticket</button>
                    </div>
                </div>
            </form>
        </t>
    </template>
</odoo>
